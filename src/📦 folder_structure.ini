📦  prisma
    └── schema.prisma               # Prisma schema file
📦 src/                             # Main application entry point for Next.js App Router
    ├── app/                        # Serves API documentation (e.g., Swagger UI)
    │   ├── api/                    # Route handlers for tRPC or REST endpoints
    │       └── api-doc/            # Serves API documentation (e.g., Swagger UI)
    │           └── page.tsx        # Page component for Swagger UI.
    │       └── contact/            # Handles contact-related API routes
    │           └── [id]/           
    │           │   └── route.ts    # Dynamic route handler for operations on a specific contact by ID.
    │           └── route.ts        # Route handler for general contact operations (e.g., list, create).
    │       └── swagger/
    │           └── route.ts        # Serves the Swagger/OpenAPI endpoint.
    ├── page.tsx                    # Root page component for the application.
    ├── lib/                        # Utility and library code for the app.
    │   └── swagger/                # Contains Swagger/OpenAPI schema and helper files.
    │       └── components.ts       # Defines reusable Swagger components (schemas, responses, etc.).
    │       └── paths.ts            # Defines API endpoint paths for Swagger.
    │       └── swagger.ts          # Main Swagger/OpenAPI configuration and export.
    |   └── prisma.ts               # Prisma client setup and database utility functions.

    